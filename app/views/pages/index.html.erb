<h1>Listing pages</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>URL</th>
  </tr>
 
  <% @pages.each do |page| %>
    <tr>
      <td><%= page.title %></td>
      <td><%= page.url %></td>
    </tr>
  <% end %>
</table>
